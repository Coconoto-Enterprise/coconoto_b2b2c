# 2026-08-12

- Added a **Team** tab to the admin dashboard (`src/pages/VintageDashboard.tsx`) showing all team members with full details.
- Created `src/components/admin/TeamPanel.tsx`: grid of profile cards (photo, name, role, email, phone, DOB, course, institution, bank/account, LinkedIn, about) with a search filter. Uses emerald/green theme to match the dashboard.
- Enriched `src/data/profiles.ts` (Profile interface + 11 entries) by merging the two CSVs the user provided:
  - `Coconoto Team form (Responses) - Form Responses 1.csv` (DOB, course, institution, bank, account, headshot)
  - `Profile Card Information Collection (Responses) - Form responses 1.csv` (role, phone, LinkedIn, description, picture)
  - Added optional fields: middleName, personalEmail, dateOfBirth, courseOfStudy, institution, bankName, accountNumber, headshotUrl.
- Wired the two new team photos the user added to `src/assets/team/`:
  - `Kehinde Elizabeth OMITOYIN.jpg` → Kehinde Elizabeth Omitoyin (Account Officer)
  - `taiwo omitoyin .jpeg` → Taiwo Esther Omitoyin (Head of Sales and Marketing)
  - Renamed the two Omitoyin profile ids to `kehinde-elizabeth-omitoyin` and `taiwo-esther-omitoyin`.
- Type-check (`tsc --noEmit`) passes cleanly.
- Fixed Vercel Hobby deploy error "No more than 12 Serverless Functions": `api/` had 14 files but only 11 are real handlers; `templateService.js` and `emailTypeMapper.js` were helper modules wrongly counted as functions. Renamed them to `_templateService.js` and `_emailTypeMapper.js` (Vercel ignores `_`-prefixed files in `api/`), updated the one import in `api/send-email.js`. Function count is now 11 (≤12), no Pro plan needed.
- Project convention: in `api/`, only files that `export default async function handler(req,res)` should be plain-named; shared helpers must be underscore-prefixed (`_helper.js`) so Vercel doesn't count them as Serverless Functions.

## Later edits (same session, continued)
- Removed `accountNumber` field from `Profile` interface + all entries; stripped year from every `dateOfBirth` (kept "Month Day").
- Removed `bankName` field entirely (interface + all entries) and removed the Bank row + unused `Banknote` icon import from `TeamPanel.tsx` — this also removes all "Opay" values.
- Verified no `bankName`/`Banknote`/`accountNumber` references remain in `src`. Build is `vite build` (no tsc step), so deploy unaffected.
