# 2026-08-11

## Team profile data cleanup (Request 7)
- `src/data/profiles.ts`: removed `accountNumber` field from the `Profile` interface and from all entries. Stripped the year from every `dateOfBirth` (kept "Month Day" only, e.g. "June 18", "July 3", "August 19").
- `src/components/admin/TeamPanel.tsx`: removed the `${member.accountNumber ? ... }` reference in the Bank detail so it shows only `member.bankName` (avoids TS error after field removal).
- Verified no remaining `accountNumber` references anywhere in `src` or non-node_modules files.
- Type-check note: `tsc --noEmit` reports ~190 pre-existing errors across the repo (mostly `React` unused-import TS6133, missing path aliases like `/src/components/Navbar`, and unresolved `supabase`/`emailService` imports). None are in `profiles.ts`/`TeamPanel.tsx`, and none are caused by this change.
- Build pipeline is `vite build` only (package.json), NOT `tsc && vite build`, so these type errors do NOT block the Vercel build/deploy — esbuild strips types.

## Env notes
- Git Bash hits a fork resource limit (`fork: retry: Resource temporarily unavailable`, exit 254) on some commands; use PowerShell for `tsc`/shell ops.
- `node_modules/.bin/tsc` is a shell wrapper — invoking it with `node` fails. Use `node_modules/typescript/bin/tsc` directly with the managed Node binary.
